// Layer 4: geometric continuity (`geom.*`).
//
// Filled in by a later commit.

import type { OdrMap } from '../../exporter/opendriveParser.js'
import type { OdrFinding, ResolvedGeometryThresholds } from '../types.js'

export function checkGeometry(_map: OdrMap, _thresholds: ResolvedGeometryThresholds): OdrFinding[] {
  return []
}
