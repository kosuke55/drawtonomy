// Layer 2: reference integrity (`ref.*`).
//
// Filled in by a later commit.

import type { OdrMap } from '../../exporter/opendriveParser.js'
import type { OdrFinding } from '../types.js'

export function checkReferences(_map: OdrMap): OdrFinding[] {
  return []
}
