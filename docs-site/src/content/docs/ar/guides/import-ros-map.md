---
title: استيراد ROS OccupancyGrid (.pgm + .yaml)
description: حمّل خريطة شبكة إشغال ROS map_server (.pgm + .yaml) — مبنية بـ nav2 أو Cartographer أو Gmapping — إلى drawtonomy كطبقة خلفية، ثم ارسم المسارات والممرات والعقبات فوقها.
keywords:
  - شرح شبكة إشغال ROS
  - محرر خرائط nav2
  - عارض خرائط cartographer
  - الرسم على خريطة pgm
  - أداة شرح خرائط SLAM
  - أداة تحرير خرائط الروبوتات
---

يفهم drawtonomy صيغة ROS `map_server` المستخدمة في [nav2](https://navigation.ros.org/) وCartographer وGmapping وأدوات SLAM المماثلة.

![شبكة إشغال ROS مستوردة إلى drawtonomy مع أسهم ورفوف مرسومة فوقها](/img/ros-occupancy-grid.png)

تُظهر اللقطة شبكة إشغال حقيقية لمستودع (الخلايا المشغولة سوداء، والحرة بيضاء) مع مسارات وعقبات مرسومة مباشرة فوقها داخل drawtonomy.

## الاستيراد

1. افتح قائمة **File** ← **Import**.
2. اختر **معًا** ملف `.pgm` وملف `.yaml` المطابق في مربع حوار الملف.
3. يقرأ drawtonomy بيانات YAML الوصفية (الدقة، العتبات) ويعرض الشبكة على اللوحة.

إن اخترت ملف `.pgm` فقط دون `.yaml`، يستخدم drawtonomy القيم الافتراضية (`resolution = 0.05 m/px` وعتبات إشغال قياسية).

## تلوين الخلايا

| الخلية | اللون |
|---|---|
| مشغولة | أسود |
| حرة | أبيض |
| غير معروفة | رمادي |

تُعرض الخلايا بمقياس يطابق أبعاد المسارات في drawtonomy، فيمكنك رسم المسارات والممرات والأشكال فوقها مباشرة — تمامًا كما في اللقطة أعلاه.

## أدوات مختبرة

استُخدِم drawtonomy مع خرائط من nav2 وCartographer وGmapping. وينبغي أن تعمل المنتجات الأخرى ما دامت تُنتج زوج `.pgm` + `.yaml` لـ `map_server` القياسي.

## انظر أيضًا

- [استيراد ملف Lanelet2 (.osm)](/ar/guides/import-lanelet2/)
