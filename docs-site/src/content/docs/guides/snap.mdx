---
title: Snap to existing geometry
description: drawtonomy snaps every drawing tool to nearby points and edges — across linestrings, lanes, vehicles, and basic shapes.
---

import Demo from '../../../components/Demo.astro';

Snapping is on by default and works across every tool. When the
cursor gets close to an existing point or edge, drawtonomy locks
onto it and lights up an indigo indicator.

<Demo src="/videos/snap-demo.mp4" caption="A single linestring is the starting target; a second linestring snaps to it, then a vehicle and a rectangle snap to features in turn." />

## Across shape types

The demo runs three different tools against the same kind of
target.

### Linestring snaps to linestring

Starting from a single vertical linestring, the second one is drawn
so its corner lands exactly on the first's top edge. The result is
a right angle that shares a point — not just visually overlapping
pixels.

![Two linestrings, the second one snapped to the first to form a corner](/img/frames/snap-linestring.png)

### Vehicle snaps to a lane

Switching to the Vehicle tool and dropping a car onto a lane snaps
the vehicle onto the lane's centerline and orients it along the
lane direction.

![A vehicle placed on a horizontal lane, snapped to its centerline](/img/frames/snap-vehicle.png)

### Rectangle snaps to a vehicle / canvas point

The Rectangle tool gets the same snap behaviour. The dashed
indicator shows where the corner is being pulled to.

![Rectangle being drawn near a vehicle; a dashed line shows the snap target](/img/frames/snap-rectangle.png)

## Disable snap temporarily

Hold <kbd>Shift</kbd> while clicking or dragging to bypass snap for
that single action. Release <kbd>Shift</kbd> to resume snapping.

This is the right move when you want a point near, but not exactly
on, an existing target.

## See also

- [Share points between shapes](/guides/point-sharing/) — when you
  want the new point to *be the same* as the target, not just
  coincident.
- [Snap & point sharing model](/explanation/snap-and-sharing/)
