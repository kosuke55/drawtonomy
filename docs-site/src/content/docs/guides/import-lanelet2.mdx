---
title: Import a Lanelet2 (.osm) file
description: Open a Lanelet2 OSM map for editing, optionally selecting only the lanes you need.
---

import Demo from '../../../components/Demo.astro';

drawtonomy can read [Lanelet2](https://github.com/fzi-forschungszentrum-informatik/Lanelet2)
OSM-format maps. Sample maps are available from the
[Autoware documentation](https://autowarefoundation.github.io/autoware-documentation/main/demos/planning-sim/#download-the-sample-map).

<Demo src="/videos/lanelet-import-demo.mp4" caption="Importing a full Lanelet2 file." />

## Import the whole file

1. Open the **File** menu → **Import**.
2. Pick the `.osm` file.
3. drawtonomy parses the file and populates the canvas with Lane shapes
   plus their connections.

## Import only specific lanes

For large maps, importing every lane will slow the editor down. Use
the lane-selection dialog instead:

<Demo src="/videos/lanelet-select-import-demo.mp4" caption="Drag-select only the lanes you care about, then import." />

1. Open the **File** menu → **Import → Select lanes**.
2. drawtonomy shows a preview map.
3. Click or drag to select the lanes you want.
4. Click **Import selection**.

## Performance tips

Keep the imported scene under roughly **500 lanes** for a responsive
editor. For larger areas, split your work across multiple files, or
import only the region of interest.

## After import

Imported lanes are full-fidelity Lane shapes — connections from the
Lanelet2 file are preserved. You can edit, [smooth](/guides/smooth-lanes/),
or [export back](/guides/export-asam/) without losing topology.

## See also

- [drawtonomy.svg format](/reference/drawtonomy-svg/) — the lossless
  format for re-editing within drawtonomy.
- [Generate lanes from a map](/guides/lane-from-map/) — when you do not
  have a curated map file.
