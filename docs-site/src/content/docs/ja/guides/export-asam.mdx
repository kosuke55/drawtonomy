---
title: OpenDRIVE / OpenSCENARIO / esmini へエクスポート
description: drawtonomy のシーンを ASAM OpenDRIVE 1.8 (.xodr)、OpenSCENARIO 1.3 (.xosc)、または esmini ですぐに再生できる zip バンドルとしてエクスポートします。
keywords:
  - 無料 OpenSCENARIO エディタ
  - 無料 OpenDRIVE エディタ ブラウザ
  - OpenSCENARIO 作成 オンライン
  - esmini シナリオ 作成
  - ASAM シナリオ 作図ツール
  - 描画 OpenDRIVE 変換
  - シミュレータ向け シナリオ
  - 自動運転 シミュレーション エクスポート
---

drawtonomy はシーンを ASAM
[OpenDRIVE](https://www.asam.net/standards/detail/opendrive/)
（`.xodr`）と
[OpenSCENARIO](https://www.asam.net/standards/detail/openscenario/)
（`.xosc`）にエクスポートできます。両者を [esmini](https://github.com/esmini/esmini) で再生できる zip にまとめることもできます。

![drawtonomy のシーンをエクスポートして esmini で再生した様子](/img/asam-esmini.png)

## エディタからエクスポート

1. シーンを描きます（レーン、車両、信号機など）。
2. **File → Export → Export for esmini** を開きます。
3. プロンプトでベース名を入力します。
4. `<name>.xodr` と `<name>.xosc` を含む `<name>.zip` がダウンロードされます。

## 再生する

```bash
unzip my-scene.zip
esmini --osc my-scene/my-scene.xosc --window 60 60 1024 768
```

## 個別の形式

どちらか一方だけが必要な場合:

- **File → Export → OpenDRIVE (.xodr)**
- **File → Export → OpenSCENARIO (.xosc)**

## プログラム経由でのエクスポート

エクスポータは `@drawtonomy/sdk` の一部で、エディタなしで動作します。CI パイプライン、サーバーサイドツール、ブラウザエクステンションなどで利用できます。

```ts
import { exporter, createSnapshot } from '@drawtonomy/sdk'

const snapshot = createSnapshot(shapes)
const { blob, baseName } = exporter.buildEsminiZip(snapshot, {
  baseName: 'my-scene',
})
```

API 全体と拡張ポイントについては、[Exporter SDK リファレンス](/ja/extend/exporter-sdk/) を参照してください。

## 出力されるバージョン

| 形式 | バージョン |
|---|---|
| OpenDRIVE | 1.8 |
| OpenSCENARIO | 1.3 |

## 関連項目

- [エクスポータアーキテクチャ](/ja/explanation/exporter-architecture/) — スナップショットからファイルまでのパイプラインの動作。
- [ターゲット形式を追加する](/ja/extend/exporter-sdk/) — CARLA、SUMO、Unity など、新しい出力先を組み込む。
